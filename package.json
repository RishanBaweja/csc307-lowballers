{
  "name": "lab1",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm --prefix react-frontend run dev",
    "build": "npm --prefix react-frontend run build",
    "preview": "npm --prefix react-frontend run preview"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "mongoose": "^8.19.1"
  }
}
